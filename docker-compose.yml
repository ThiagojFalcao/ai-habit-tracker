services:
  mongo:
    image: mongo:8.0
    container_name: ai-habit-tracker-mongo
    ports:
      - "27018:27017"
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
